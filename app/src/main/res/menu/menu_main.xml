<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/action_good"
        android:icon="@drawable/ic_action_good"
        android:title="@string/action_good"
        app:showAsAction="always"/>

    <item android:id="@+id/action_favourite"
        android:icon="@drawable/ic_action_favorite"
        android:title="favouriteActnButton"
        app:showAsAction="always"/>

    <item android:id="@+id/action_bad"
        android:icon="@drawable/ic_action_bad"
        android:title="badActnButton"
        app:showAsAction="always"/>


    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/action_item"
        android:title="shared new item!"
        android:orderInCategory="100"
        app:showAsAction="never" />
</menu>
